# Copy to .agent-secret.env, set mode 0600 and replace both values.
# Never commit the populated file.
WOODPECKER_AGENT_SECRET=replace-with-control-plane-agent-secret
WOODPECKER_GRPC_SECRET=replace-with-control-plane-grpc-secret

